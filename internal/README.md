# Cognitive Nexus Internal Files

This folder is reserved for protected backend prompt/config notes.

Runtime chat messages should not write here. User-facing persona preferences live in
`data/chat_profile.json`; protected backend behavior lives in code under `modules/`.
